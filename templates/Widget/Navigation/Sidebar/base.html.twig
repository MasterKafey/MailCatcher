<div class="flex grow flex-col gap-y-5 overflow-y-auto bg-secondary px-6 pb-2 ring-1 ring-white/10">
    <div class="flex h-48 shrink-0 items-center justify-center">
        <img class="h-24 w-auto" src="{{ asset('images/logo.svg') }}" alt="MailCatcher's logo">
    </div>
    <nav class="flex flex-1 flex-col">
        <ul role="list" class="flex flex-1 flex-col gap-y-7">
            <li>
                <ul role="list" class="-mx-2 space-y-1">
                    {% for item in sidebar_business.items('authenticated') %}
                        {% include 'Widget/Navigation/Sidebar/item.html.twig' with item %}
                    {% endfor %}
                </ul>
            </li>
            <li class="-mx-6 mt-auto">
                <div class="flex items-center gap-x-4 px-6 py-3 text-sm font-semibold leading-6 text-white hover:bg-gray-800">
                    <img class="h-8 w-8 rounded-full bg-gray-800" src="{{ app.user.email|gravatar_image }}"
                         alt="{{ app.user.email }}'s profile picture">
                    <span aria-hidden="true">{{ app.user.email }}</span>
                </div>
            </li>
        </ul>
    </nav>
</div>